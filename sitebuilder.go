package site_builder

type Renderer struct {
	// contextSource should be an interface that provides context that the template package can
	// inject into templates.
	//contextSource someTypeOfThing
}
